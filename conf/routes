
# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# An example controller showing a sample home page
GET     /                       controllers.HomeController.index

# Map static resources from the main resources folder to the root URL path
GET     /*fileName.html         controllers.HtmlController.display(path = "src/main/resources/static/pages", fileName, ext = "html")
GET     /*fileName.js           controllers.HtmlController.display(path = "src/main/resources/static/js", fileName , ext = "js")

# Redirect all unrouted requests to the bad request page
GET     /*other                 controllers.ErrorController.badRequest(other)