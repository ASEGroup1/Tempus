
# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# An example controller showing a sample home page
GET     /                       controllers.HomeController.index

# Map static resources from the main resources folder to the root URL path
GET     /*fileName.html         controllers.HtmlController.displayFile(path = "src/main/resources/static/pages", fileName, ext = "html", contentType = "text/html")
GET     /*fileName.js           controllers.HtmlController.displayFile(path = "src/main/resources/static/js", fileName , ext = "js", contentType = "text/javascript")

# Map all static resources with unknown extentions to be rendered thier without MIME specified
GET     /*fileName              controllers.HtmlController.display(path = "src/main/resources/static", fileName , contentType = "")